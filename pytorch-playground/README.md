I'll be using this repo to upload smol code snippets and tutorials for PyTorch objects I find interesting. Filed into `geometric` (graphs), `ignite` (the regular one) and `lightning` (the cool one). 
